"use client";
import { useRouter } from "next/navigation";
import { AppShell } from "@/components/app-shell";
const actions = [{ href: "/productos", icon: "▦", title: "Productos y stock", desc: "Ver existencias y alertas" }, { href: "/cargar/ingreso?kind=general", icon: "↗", title: "Cargar ingreso", desc: "Ingreso general o venta simple" }, { href: "/cargar/egreso", icon: "↘", title: "Cargar egreso", desc: "Gasto, retiro o préstamo" }, { href: "/cargar/ingreso?kind=debt", icon: "♙", title: "Cobro de deuda", desc: "Registrar un cobro pendiente" }, { href: "", icon: "◇", title: "Promo", desc: "Próximamente", disabled: true }];
export default function CargarPage() { const router = useRouter(); return <AppShell title="Cargar" showFab={false}><div className="section-title"><h2>¿Qué querés registrar?</h2></div><div className="action-grid">{actions.map((item) => <button className="action-card" key={item.title} disabled={item.disabled} onClick={() => item.href && router.push(item.href)}><div className="action-icon">{item.icon}</div><div><strong>{item.title}</strong><span>{item.desc}</span></div><span style={{ marginLeft: "auto", color: "var(--muted)" }}>{item.disabled ? "🔒" : "›"}</span></button>)}</div><div className="alert info" style={{ marginTop: 22 }}>Los registros MVP 1 se guardan en Notion cuando la conexión está configurada.</div></AppShell>; }
