"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function LoginPage() { const router = useRouter(); const [pin, setPin] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setLoading(true); setError(""); const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ pin }) }); const body = await response.json(); setLoading(false); if (!body.ok) return setError(body.error?.message || "No se pudo iniciar sesión."); router.replace("/"); }
  return <main className="login-page"><section className="login-card"><div className="brand-mark">₲</div><h1>Finanzas El Tigre</h1><p>Tu resumen financiero, movimientos y cuentas en un solo lugar.</p><form onSubmit={submit}><div className="form-field"><label htmlFor="pin">PIN de acceso</label><input id="pin" autoFocus inputMode="numeric" type="password" value={pin} onChange={(e) => setPin(e.target.value)} placeholder="Ingresá tu PIN" maxLength={12} /></div>{error && <div className="alert error">{error}</div>}<button className="primary-btn" disabled={loading || !pin}>{loading ? "Entrando…" : "Entrar"}</button></form><p className="small" style={{ margin: "18px 0 0", textAlign: "center" }}>En modo demo, el PIN es <strong>1234</strong>.</p></section></main>;
}
